export class aboutDataType {
    constructor (slug:string="", title:string="", description: string=""){
        this.slug=slug;
        this.title=title;
        this.description= description;
    }
    slug: string;
    title: string;
    description: string;
}